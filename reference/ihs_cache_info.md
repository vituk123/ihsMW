# Display Information About Cached IHS Data

Scans the internal `ihsMW` cache directory and reports on any previously
downloaded datasets.

## Usage

``` r
ihs_cache_info()
```

## Value

A `tibble` summarizing the cached files.

## Examples

``` r
if (FALSE) { # \dontrun{
ihs_cache_info()
} # }
```
